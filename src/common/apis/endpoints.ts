// Root
const authRoot = 'auth';

export const routes = {
  auth: {
    root: authRoot,
    authentication: {
      option: `authentication/option`,
      verify: `authentication/verify`,
    },
  },
};
